# 更新日誌

`desktop-v0.1.9` 到 `desktop-v0.1.13` 之間所有值得記錄的改動。

> **從 npm 安裝的話，先讀這段。** `0.1.9` 到 `0.1.12` 有 tag、也建置過，但**從來沒有進到 registry** —— 那幾版根本打包不起來（見 [0.1.13 → 發布工程](#發布工程)）。npm 上是從 `0.1.8` 直接跳到 `0.1.13`，所以 0.1.9–0.1.12 底下列的每一項，對 npm 使用者來說都是在 **0.1.13** 才第一次拿到。
>
> 桌面安裝檔是另一條時間線：到 `0.1.8` 為止的每一次發布都把安裝檔建好又丟掉，**0.1.9** 才第一次真的出現在 Releases 頁上。

---

## 未發布

### 新增

**Univer Office 開在右欄。** 試算表、文件與簡報渲染在殼的右側欄，而不是浮在對話上方的視窗裡：文件在它開著的期間就是工作發生的地方，而蓋住逐字稿的視窗會逼你在「讀 agent 說了什麼」和「看它做出了什麼」之間二選一。這一欄同時只有一個佔用者 —— 有文件時是文件，否則是工具細節 —— 而且在沒有當前 session 時仍然保持開啟，因為文件的壽命比產生它的那一輪長。

這個插件是 vendor 進來的，所以安裝這個產品不會透過它的 peer dependencies 再下載一份上游 harness。它的三個 runtime 依賴完全沒有發布授權；在條款與發布方確認之前，`THIRD_PARTY_NOTICES.md` 會照實揭露這件事。

---

## 0.1.15 — 2026-08-28

0.1.14 從乾淨安裝開不起來。這一版是那個修正，外加兩個本來就該擋下它的檢查。

### 修正

**安裝後的樹在應用啟動之前就死了**，訊息是 `Cannot find module …/lib/types-Dc6T3R7E.js imported from …/lib/fs.js`。`@unieai/uad-execution-router` 有四個共用同一個模組的進入點；打包器把它切成一個檔名帶內容雜湊的 chunk，而帶雜湊的檔名沒辦法寫進 `files` —— 於是那個 chunk 從來沒被發布，套件連自己的進入點都 import 不了。**0.1.13 也是這樣出貨的**；它一直看不見，是因為 0.1.14 修掉的那個 forwarder 當機會先殺掉開機，也因為這裡每一個測試解析的都是 `src` 而不是 `lib`。

那個 chunk 現在有固定檔名，也被發布了。用固定檔名而不是每個進入點各自打包：共用模組匯出一個類別，把類別複製到各個進入點，會讓兩個 row 拿到兩份 —— 那是識別失敗，不只是體積問題。

### 發布工程

- **`verify-published-emitted-files`**：每一個建置後的套件在 `lib/` 底下產出的 `.js`，都必須被它自己的 `files` 清單發布。其他東西都看不到這件事 —— 測試讀 `src`、publint 讀 manifest 而不是產出的目錄樹、而打包安裝探針只載入一個進入點就返回了。已掛進 `hygiene`。
- **打包安裝探針現在會真的開機**，不只是跑執行檔：它用 port 0 啟動 web profile 並等它宣告位址，這會套用組合裡的每一個進入點。原本的 `--version` 在 loader 掛載任何東西之前就返回了，這就是一個壞掉的套件連續兩次通過發布的原因。探針也不再省略 optional 依賴，因為真實使用者會裝它們 —— 省略反而讓那棵樹比任何使用者的都嚴格。
- 這個 repo 長期全紅的 hygiene 閘門有兩個重新變綠：publint 與 built package invariants 一直在回報這件事，紅太久所以沒人讀。

---

## 0.1.14 — 2026-08-28

修正 0.1.13 在每一次 `bunx`／`npx` 啟動都會遇到的當機，以及消掉它成因的封裝改動。

### 修正

**`bunx @unieai/rabi web` 在應用啟動之前就死了**，訊息是 `dsh: …/.dsh/profiles/node_modules/@deepseek-ai/dsh-client-runtime exists and is not a symlink`。它拒絕碰的那個目錄，正是 harness 自己寫出來的。0.1.13 加進了 GenUI 插件，那個插件是對著上游 harness 發布的，宣告了十三個 `@deepseek-ai/*` **peer** 依賴 —— 而 npm 7+ 與 bun 都會自動安裝缺少的 peer，於是安裝這個產品會下載一整份第二個 harness。開機時的模組 fallback 走訪找到那些套件，試圖在某個 forwarder 的路徑上建立連結。

當機只是看得見的那一半。如果那個路徑當時是空的，插件會解析到一個它從來沒有被載入其中的 harness 的 `Context` 類別與服務註冊表：任何服務都拿不到，而且是無聲的。現在除非 app 自己宣告，上游名稱一律留給它的 forwarder；而 harness 自己產生的 forwarder 會被取代而不是拒絕 —— 所以已經處在壞狀態的機器，下一次啟動就會自己治好。

### 變更

**GenUI 被 vendor 進來了，所以那第二份 harness 根本不會被下載。** 安裝這個產品不再多拉 184 個套件（其中 73 個是 `@deepseek-ai/*`）；`pnpm-lock.yaml` 裡一個都沒有。這個插件以原始碼形式釘在 `vendor/genui/`、以 `@unieai/genui` 重新發布，而且順道從上游 0.9.3 更新到 **0.9.6**：面板新增模板中心與探索成就，另有 quiz 字串選項、表格入場動畫閃屏、plot 滾輪縮放穿透、窄卡片評分版面與剪貼簿回退等修正。

`pnpm run sync-vendor-genui <version>` 負責之後的更新，並且會斷言那兩個必須在網路兩端指向同一個套件的常數 —— 弄錯一個，fence 照樣畫得出來，但每一個重量級渲染器都會 404。

### 發布工程

- 工作區閘門 `check-workspace-constraints` 重新變綠，而且這一版它什麼都沒抓到：它先前遮住的既有問題都在 0.1.13 修掉了。

---

## 0.1.13 — 2026-08-27

遠端機器這一版：harness 現在可以跑在另一台不是這裡的電腦上，而且從輸入框旁邊就能選。這也是 0.1.8 之後第一個真正進到 npm 的版本。

### 新增

**把工作跑在另一台機器上。** 輸入框旁的機器選擇器會把整個執行世界 —— bash、檔案樹、終端機、工作區 —— 透過同一條 SSH 連線導到你選的那台機器。機器清單來自你本來就連得到的那些（`~/.ssh/config`、agent keys），也可以直接在選擇器裡新增和移除。harness 是被**組合到**那台機器上，而不是假設它在本機，所以同一個 session 裡的指令、檔案讀取和終端機都會落在同一個地方。

**機器狀態。** 選擇器會顯示執行工作的那台機器實際在做什麼 —— CPU、記憶體、磁碟 —— 讓「為什麼這麼慢」有一個不是用猜的答案。

**Skills 頁。** 列出這個 build 真正提供的 skill，而不是設定檔宣稱的那些。也可以一鍵把 skill 從你的 UnieAI 帳號複製到這台機器上。

**能編輯，不只是能看。** 檔案打開就能編輯，有語法高亮，`Cmd`/`Ctrl`+`S` 存檔。

**內建三個 skill。** `find-skill`、`skill-creator`、`brainstorming` 每個安裝都找得到，不需要任何設定。

**GenUI。** `@changfenhuang/dsh-genui` 進了 web-app bundle：模型可以用 ` ```dsh-ui ` fence 回答 —— 版面、圖表、表單、mermaid、3D —— 而且有一條回到模型的互動迴路。它是一個以上游 harness 名稱發布的社群插件，所以把它裝進來同時也證明了上游插件在這個 fork 上不用改就能跑。

**Studio 知識庫引用。** Studio 的 MCP 回答現在會在該次呼叫的 Output 底下顯示它從哪裡來 —— 哪份文件、哪一頁、搜尋的匹配程度有多強。

**會對 session 有反應的吉祥物。** 預設關閉；它讀的是你本來就在看的那份 session 快照，所以它反映的是這一輪的狀態，而不是照著計時器動。

**思考球。** 模型工作時，原本那個靜態的原子圖示換成產品的思考球動畫，結束後回到靜態圖示。

**能證明自己拍到頁面的截圖。** `page_screenshot` 加了 `waitForText` 和另外三個選項：等待計時器回答的是「時間夠了嗎」，永遠不是「東西在了嗎」，而一個持續開著串流的應用根本不會進入 network-idle。

### 修正

- **檔案樹在整個 session 期間是凍結的** —— 第一次載入之後就再也沒有重讀。
- **瀏覽另一台機器時，資料夾選擇器顯示的是這台電腦的資料夾。**
- **額度數字是啟動當下的樣子**，之後不會更新。
- **SSH 斷線被當成指令執行完畢** —— 連線中斷現在會明確失敗，而不是看起來像成功。
- **router 建出來的 provider 從來沒有經過插件生命週期**，所以執行期選到的機器可能被導向一個根本沒被啟動的 provider。
- 工作區路徑改為透過檔案系統接縫做正規化，所以 symlink 或相對路徑在每一台機器上都解析成同一個結果。

### 發布工程

- **整個 dsh family 根本打包不起來。** 發布負載閘門把 `src/` 底下的每一條路徑都擋掉，而 `@unieai/uad-client-ui-primitives` 是刻意發布 `src/vendor/*/LICENSE` 的 —— 那是 vendored 元件的授權要求發行物必須攜帶的聲明。`release:pack` 在那個套件上直接拋錯，這就是 0.1.9 到 0.1.12 有 tag 卻從沒發布的原因。授權文字不是原始碼；這個例外比對的是完整檔名，所以 `src/LICENSE.ts` 仍然算原始碼。
- **兩個套件 npm 只肯當成私有套件收。** `@unieai/uad-client-ui-pet` 和 `@unieai/uad-studio-kb-sources` 沒有 `publishConfig.access`，npm 於是預設它們是 restricted 並回 `E402 Payment Required`。發布跑到那裡停住，255 個套件裡已經上去 177 個 —— 一次發布是一個整體，而這個失敗留下的正是「一半在 registry 上」的狀態。
- **工作區閘門噪音太大，所以沒人看。** `check-workspace-constraints` 每次執行都印出約 250 個錯誤 —— 每個 release member 一個，全部都是這個 fork 改名時沒跟著改的 repository URL。上面那個少掉的 `publishConfig` 之所以能一路走到發布，就是因為這個。常數修好了，它一直遮住的東西也一併修掉：六個還指著上游的 manifest、兩個沒有發布型別宣告的套件、一個帶著 `cordis.patch.yml` 卻沒宣告它的 bundle、三個發布了 files 政策不知道的進入點 bundle，以及 `apps/desktop` —— 一個以安裝檔形式發行的 Electron 應用 —— 一直被要求必須可發布。
- **會跑的 CI。** 繼承來的 `ci.yml` 要求的是這個 fork 並不擁有的私有 runner pool，所以它一次都沒跑過：五十個 commit、四次桌面發布，沒有任何自動檢查。

---

## 0.1.12 — 2026-08-26

### 新增

**兩種 provider 放在同一頁，而且說清楚哪個是哪個。** **API Provider** 那一列的金鑰放在產品的伺服器上，由帳號計量，而且會跟著你到每一個登入的用戶端。**這台機器上**那一列的金鑰放在本機的 `.credentials.yaml`，直接呼叫端點，不被任何人計量。兩種都會出現在同一個模型選單裡，所以輸入框把它們並排提供，卻沒有任何東西說明這則訊息會從哪一邊計費。

**在設定裡選視覺模型**，從那些宣告自己看得見的模型中選。`image_inspect` 要委派給哪一條路徑，以前寫在 cordis config 裡 —— 要改 patch 檔、還要重啟。現在它是一個設定，即時生效，而且只有**明確宣告**支援圖片輸入的才算：adapter 沒說話的模型會缺席，而不是被猜成不支援。

### 修正

- **Apple Silicon 打包工作在 34 秒時失敗，而且說不出任何原因。** 原本單一的 `Package` 步驟拆成三個 —— 確認 runner 就是目標、建置 shell、打包並發布 —— 這樣下一次失敗自己會說出是哪一段。兩個 Mac 工作也搬到同一個映像上，讓它們之間唯一的差別回到架構本身。

---

## 0.1.11 — 2026-08-26

### 新增

**看不見的模型也可以問關於那張圖的問題。** 把圖片附給純文字模型原本會整則訊息被拒（`MODEL_DOES_NOT_SUPPORT_IMAGES`）—— 那正是 `image_inspect` 被做出來要處理、卻永遠碰不到的情況，因為拒絕發生在模型看到任何東西之前。當有設定視覺路徑時，附件會被收下，模型拿到的是一個它可以自己提問去檢視的替身。只有它真的問了，才會發生一次視覺呼叫；沒有註冊這個工具時，一切照舊。

### 修正

- **打包後的應用解析不到自己的插件。**

---

## 0.1.10 — 2026-08-26

### 修正

- **套件承諾的 chromium face 從來沒被建置出來**，也沒有任何東西檢查它有沒有被建出來。現在會建，而且有閘門。
- **桌面產物的檔名是檔案系統不接受的。** 每個目標帶著一個檔名安全的 `slug`（`mac-arm64`、`win-x64`…），閘門同時檢查產物名稱必須來自 `slug`、以及每個 slug 都符合安全樣式。拿當初失敗的那個名字做對照執行：紅的。

---

## 0.1.9 — 2026-08-26

第一個桌面安裝檔真的出現在 Releases 頁上的版本。

### 新增

**`page_screenshot`** —— 模型可以拍一張頁面的照片。它為了一次呼叫啟動瀏覽器然後丟棄：全新的 profile、全新的行程，在工具回傳之前就拆掉，所以沒有任何 session 會把某一頁的 cookie 帶進另一頁的照片裡。只有 `http`/`https` 可以擷取，其他 scheme 在瀏覽器啟動之前就被拒絕。

**`image_inspect`** —— 把關於一張圖的一個問題交給設定好的視覺路徑，拿回它的文字；這一輪本身仍然用自己的模型。沒有指定路徑時它以休眠狀態掛載，所以一個沒有視覺模型的部署提供的是「沒有這個工具」，而不是一個每次呼叫都失敗的工具。

### 修正

- **四次綠燈、四乘四個安裝檔，然後哪裡都沒有 release。** Package 步驟在一個結尾已經是 `--publish never` 的腳本後面接了 `-- --publish always`；pnpm 會把 `--` 原封不動傳下去，而 yargs 在遇到它時就停止解析選項 —— 於是 `publish` 一直是 `never`，那個覆寫參數躺在 `argv._` 裡不起作用。0.1.5 到 0.1.8 的每一次執行都把安裝檔建好又丟掉，而且每次都以 0 結束。現在旗標由腳本自己帶，步驟不再追加任何東西，安裝檔同時也上傳成 run artifact —— 因為當初如果有它們在，「輸出到底在哪裡」這個問題根本不會需要問。
- **Intel macOS 的工作等了一小時，等一個已經不存在的 runner。** GitHub 淘汰了 macOS 13 映像，而一個標籤背後沒有 runner 的工作不會失敗 —— 它會排隊。所以 0.1.8 只帶著四個安裝檔中的三個發出去，而且沒有任何錯誤可以讀。
- **未登入的視窗會先問你要不要去登入頁。** 現在直接去。那張卡片留下來當備援，處理跳轉沒送達的兩種情況 —— 因為一個每次回來都把你踢出去的視窗，比一顆按鈕更糟。

> **給下一個發桌面版的人：** electron-builder 的 GitHub provider 建立的是**草稿** release，而 `electron-updater` 只從已發布的 release 讀取更新來源。草稿代表檔案拿得到，不代表更新會動。

---

## 安裝

```bash
npm install -g @unieai/rabi@0.1.15
```

macOS（Intel 與 Apple Silicon）與 Windows 的桌面安裝檔附在每一個 release 上。
