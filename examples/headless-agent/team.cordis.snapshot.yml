# Keyless Agent Teams composition over the real headless app and deterministic fixture adapter.
- id: base
  name: '@unieai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@unieai/uad-llm-deepseek'
        disabled: true
      - id: tool-subagent-control
        name: '@unieai/uad-tool-subagent-control'
        disabled: true
      - id: tool-subagent-report
        name: '@unieai/uad-tool-subagent-report'
        disabled: true
      - id: tool-subagent
        name: '@unieai/uad-tool-subagent'
        config:
          provider: spawn
          toolName: subagent
          backgroundMode: one-shot
          maxDepth: 1
      - id: tool-subagent-fork
        name: '@unieai/uad-tool-subagent'
        config:
          provider: fork
          toolName: subagent_fork
          backgroundMode: one-shot
          maxDepth: 1
      - insert:
          - id: agent-team
            name: '@unieai/uad-experimental-agent-team'
          - id: tool-agent-team
            name: '@unieai/uad-experimental-tool-agent-team'
          - id: team-fixture-llm
            name: './tests/fixtures/team-llm.mjs'
