# Keyless replay counterpart to pty.cordis.yml.
- id: base
  name: '@unieai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@unieai/uad-llm-deepseek'
        disabled: true
      - insert:
          - id: pty
            name: '@unieai/uad-terminal'
          - id: pty-snapshot-backend
            name: './pty-snapshot-backend.mjs'
          - id: tool-terminal
            name: '@unieai/uad-tool-terminal'
            config:
              maxResultBytes: 64
          - id: llm-replay
            name: '@unieai/uad-llm-replay'
            config:
              providers:
                - id: deepseek-official
                  name: DeepSeek
                  models:
                    - id: deepseek-v4-flash
                    - id: deepseek-v4-pro
