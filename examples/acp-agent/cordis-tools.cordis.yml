# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@unieai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@unieai/uad-cordis-host-runner'
          - id: tool-cordis
            name: '@unieai/uad-tool-cordis'
